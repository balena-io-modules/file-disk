{
  "name": "file-disk",
  "version": "4.1.1",
  "description": "Handles reads / writes on disk image files.",
  "author": "Petros Angelatos <petrosagg@resin.io>",
  "contributors": [
    "Alexis Svinartchouk <alexis@resin.io>",
    "Petros Angelatos <petrosagg@resin.io>"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/resin-io-modules/file-disk"
  },
  "bugs": {
    "url": "https://github.com/resin-io-modules/file-disk/issues"
  },
  "license": "Apache-2.0",
  "main": "build/index.js",
  "homepage": "https://github.com/resin-io-modules/file-disk",
  "scripts": {
    "clean": "rm -rf build",
    "compile": "npm run clean && tsc",
    "prepublishOnly": "npm run compile",
    "pretest": "resin-lint --typescript 1 src/*.ts test/*.ts && ./test/start_minio.sh",
    "test": "mocha --require ts-node/register test/*.ts; ./test/stop_minio.sh"
  },
  "files": [
    "build/*.js",
    "build/*.js.map",
    "build/*.d.ts"
  ],
  "types": "build/index.d.ts",
  "devDependencies": {
    "@types/bluebird": "^3.5.20",
    "@types/mocha": "^2.2.48",
    "@types/node": "^9.4.6",
    "aws-sdk": "2.39.0",
    "mocha": "^5.0.1",
    "resin-lint": "^1.5.0",
    "ts-node": "^5.0.0",
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "blockmap": "^3.0.0",
    "bluebird": "^3.5.1"
  }
}
